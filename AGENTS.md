# AGENTS.md

## 项目目标

本仓库用于开发和优化机器人导航算法。当前重点是让算法结构清晰、容易理解、容易定位问题，而不是建设完备的商用软件体系。

优先保证：

1. 能从项目结构看懂算法由哪些部分组成。
2. 能从函数名和简短说明知道每个函数的作用。
3. 能沿着数据流快速定位算法问题。
4. 文档简洁、面向人类，并足以帮助理解和使用项目。

优化以真实流程是否更容易理解、算法修改是否更容易定位、实现是否更直接为标准；不以代码行数、函数长度或文件数量单独判断质量。

## 开发设备

- Arch Linux on WSL2；NVIDIA GeForce RTX 3060 12 GB。
- NVIDIA 驱动可用且报告 CUDA 13.3，但 CUDA Toolkit/nvcc 尚未安装。
- Codex 受限沙箱可能隐藏 /dev/dxg，不能据此判断无 GPU/CUDA。

## 协作与交付

- 用户决定算法目标、重要取舍和验收判断；Agent 负责实现质量、相关旧逻辑清理和证据核对，不把逐函数审查的责任交给用户。
- 工作按“明确行为 → 分段实现 → 清理替代逻辑 → 展示关键代码 → 在授权范围内验收 → 推进相关改动”组织。小修改直接完成；大任务按可独立解释、检查和回退的行为分段，不按文件机械拆分。
- 每段说明新增了什么概念、替代了什么逻辑；实质流程变化用一个具体场景串起关键代码、正常路径与重要失败分支，帮助用户保持理解。
- 交付前检查本次替代的旧实现、参数、入口和文档是否已处理，审查新增状态、通用选项与包装是否必要；无关问题单独指出，不顺带扩大任务。
- 收尾简要说明变化及其价值、关键证据和未确认项，区分“代码已实现”“静态检查完成”“运行验收通过”；不以文件列表或净减行数代替质量证据，也不机械输出长检查报告。
- 已确定的原则和授权不反复询问，不把每次任务变成长篇访谈；只讨论无法从已有共识推断、且会实质影响结果的选择。

## 编码前流程

- 每个新任务首次修改代码前，阅读当前有效的 `AGENTS.md`、`README.md`、与本次任务直接相关的 `docs/` 文档和代码。同一任务中已读且未变化的内容可以复用；任务范围变化、文件更新或上下文不足时，补读相关部分。
- 核对文档描述与当前实现是否一致，开始修改前简要说明本次目标、准备修改的模块或文件、修改边界以及可能需要同步的文档。说明本身不构成审批步骤；除非触发明确的确认条件，否则直接继续。
- 用户已明确要求或批准的行为、接口调整，视为已授权范围，不因其与旧实现或旧文档不同而重复确认。
- 只有发现超出已授权范围的变更，或无法可靠推断且会实质改变结果的关键选择时，才请求确认；只暂停依赖该决定的部分，继续完成其他已授权工作。
- 结构优化默认保持当前导航策略、执行顺序和恢复行为；超出已授权范围的算法行为变化单独讨论，不混入可读性修改。
- 修问题先沿“输入 → 决策 → 执行反馈 → 状态更新”定位首次偏离预期的步骤；证据不足时先收集必要信息，不直接增加兜底分支。只修改当前任务所需内容；新增依赖前先征得用户同意。
- 实现完成后再次对照相关文档检查改动。
- 代码导致架构、数据流、接口、运行方式或已记录行为发生变化时，同步更新已有文档；没有文档事实变化时不要为了形式修改或新增文档。

## 架构与职责

- 按算法职责和数据流划分模块，保持清晰的依赖方向；具体模块映射见 `README.md` 和架构决策文档。
- 真机与仿真通过 Adapter 统一输入输出，共用算法和运行装配；设备专属准备留在环境或设备层。
- 启动层负责配置、组件创建和资源生命周期；运行层负责取帧、调用感知、执行动作和记录结果。
- 搜索核心负责行为选择与可恢复失败的处理，不读取设备、请求模型或执行文件读写。感知模块负责采样、队列、模型分析与定位，不控制底盘、不修改搜索状态。
- 日志和可视化只记录过程，不参与决策；行为通过显式数据表达，不解析调试字符串来驱动执行。

## 函数与实现

- 总入口展示完整的主要阶段，行为入口展示重要分支与恢复规则，计算函数直接表达具体算法。
- 主要模块优先展示入口和公共操作，再展示内部实现；数据类型与定义依赖按需要排列。`_` 只表示内部接口，不用于区分流程与计算。
- 一个函数承担一个明确职责，名称直接表达它做什么或返回什么。不机械拆分函数，不保留没有实际价值的纯转发。
- 固定流程尽量顺序表达，避免多层回调或包装遮住执行顺序；必要的资源管理和并发同步应保留。
- 内部实现以正确、直接、易读为准，不为潜在需求增加复杂抽象、通用框架或兼容层。
- 只维护项目实际使用的能力。确认仓库内无消费者、也未被用户指定保留的 Python 接口、类型、参数和通用分支可以删除，并同步调用与文档；不为未知的仓库外调用者保留兼容层。
- 简单且用途不同的少量代码允许重复；同一算法规则和同一数据解释集中维护，不为消除所有重复而抽象。
- 对作用不直观的函数，用简短注释说明用途、输入输出、关键假设和原因，不逐句翻译实现或解释显而易见的细节。

## 数据与错误处理

- 输入输出尽量显式，跨周期状态有明确归属，避免隐藏的全局状态和难以追踪的副作用。
- 新增状态时说明它记录的事实、更新者、失效时机，以及现有数据为何不能表达；新增抽象须有实际用途。这是 Agent 的实现责任，不构成逐字段审批。
- 同一事实尽量只维护一份数据；确有必要的缓存、快照或派生数据应有明确用途与更新时机，不把含义不同的数据误当成重复状态。
- 关键中间结果应容易查看，使问题能够沿“输入 → 中间步骤 → 输出”逐步排查。
- 坐标系、时间戳、物理单位和关键参数应在使用处表达清楚。
- 外部数据在配置、设备、文件或模型边界校验；内部信任已声明的数据契约，不逐层重复校验内部状态、不兼容未使用的输入形式。
- 深度缺测、地图未知、目标不可达是正常算法分支；真实设备差异、异步时序与失败恢复不能仅因代码较长就删除。
- 异常只在能够明确恢复、释放资源或跨线程／进程传递的位置捕获；不得把程序错误吞掉或伪装成无结果。保留真机动作取消、终态确认与停止约束。

## 测试

- 除非用户明确要求，否则不要编写、修改或运行任何测试，也不要搭建测试框架。
- 不主动运行已有测试、仿真、基准测试、覆盖率检查或手动测试。
- 读取代码和日志、检查差异，以及不执行项目业务代码的静态语法审阅，不属于本节所禁止的测试。
- 用户要求测试时，只执行其要求的范围；除非用户同时要求，不额外补充测试代码或测试体系。
- Skill 中的验证要求不得覆盖本节。没有测试授权时，完成其他已授权工作；不得擅自试跑脚本、启动仿真或执行硬件验证。
- 默认未测试，完成任务时明确说明这一点，不得暗示代码已经通过验证。
- 涉及导航行为的重要阶段应约定明确的验收范围：用户运行后由 Agent 分析日志，或由用户明确授权 Agent 执行指定验证；本约定本身不授予测试权限。
- 运行验收未完成时，不报告该阶段已整体验收。验收缺口影响后续工作的可信度时，优先处理相关验收与结果，暂缓依赖该结论的改动；其他独立且已授权工作继续。

## 提交

- 用户要求提交时，按职责与依赖组织提交，便于独立审阅和回退；排除无关的本地文件、用户数据与凭据。未获授权不推送远端。

## 文档

文档分为面向人类和面向 Agent 两类，不要把两类内容混写在同一份文档中。

### 面向人类的文档

- 以 `README.md` 和常规项目文档为主，帮助读者理解项目目标、模块职责、算法流程以及如何运行和调试。
- 保持简要、连贯和易读，只解释当前有效的设计，不复制代码实现，不记录开发过程。
- 架构文档描述模块关系、主要数据流和关键接口；局部函数细节留在代码附近。
- 优先使用短段落和小型示意图。只有图表能明显降低理解成本时才使用。
- 更新已有文档而不是新建重复说明；除非用户明确要求，不创建进度报告、阶段总结或逐文件说明。

### 面向 Agent 的文档

- `AGENTS.md` 只保存工作规则和索引，具体排错资料按需放在 `docs/agent/` 中。
- 每次任务结束时，由主代理检查相关 Agent 文档是否需要同步。只有新增了符合记录条件的事实，或已有内容失效，且本次任务允许修改文件时才更新。纯问答、只读审阅，或没有符合上述条件的变化时不修改。
- Agent 文档服务于定位和修复问题，可记录代码路径与职责映射、输入输出、关键数据流、不变量、参数位置、运行命令和常见故障线索。
- 使用简短列表、表格、准确路径和命令，指向源代码而不是复制大段实现。
- 只记录经过确认、难以从代码直接推断并且会重复用于排错的信息；不要写推测、讨论过程或原始日志。
- 当代码路径、接口或命令变化时同步更新或删除过时内容，避免 Agent 被旧信息误导。
- 如果现有代码和 `AGENTS.md` 已足够排错，不要为了形式创建新的 Agent 文档。

## 文档索引

- `README.md`：项目入口、架构、阅读顺序和环境索引。
- `docs/adr/`：导航行为保留、搜索与执行边界、感知归属等架构决策。
- `docs/algorithm.md`：当前算法状态机、Frontier、VLM 调用与恢复规则。
- `docs/chassis-interface.md`：Adapter 必须遵守的数据、坐标和执行契约。
- `docs/habitat.md`：Habitat 环境、GPU 启动和仿真运行。
- `docs/agent/habitat.md`：Habitat 启动顺序与 Rerun/GPU 快速隔离。
- `docs/hermes.md`：Hermes + D435i 安装、外参配置、运行、转发与排错。

`docs/slamtec-l515.md`、`docs/s100-l515.md`、`docs/orangepi.md` 已随范围收敛失效，
仅保留指向当前文档的说明，不再作为事实来源。
