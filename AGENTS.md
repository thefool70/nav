# AGENTS.md

## 项目目标

本仓库用于开发和优化机器人导航算法。当前重点是让算法结构清晰、容易理解、容易定位问题，而不是建设完备的商用软件体系。

优先保证：

1. 能从项目结构看懂算法由哪些部分组成。
2. 能从函数名和简短说明知道每个函数的作用。
3. 能沿着数据流快速定位算法问题。
4. 文档简洁、面向人类，并足以帮助理解和使用项目。

## 开发设备

- Arch Linux on WSL2；NVIDIA GeForce RTX 3060 12 GB。
- NVIDIA 驱动可用且报告 CUDA 13.3，但 CUDA Toolkit/nvcc 尚未安装。
- Codex 受限沙箱可能隐藏 /dev/dxg，不能据此判断无 GPU/CUDA。

## 编码前流程

- 每个新任务首次修改代码前，阅读当前有效的 `AGENTS.md`、`README.md`、与本次任务直接相关的 `docs/` 文档和代码。同一任务中已读且未变化的内容可以复用；任务范围变化、文件更新或上下文不足时，补读相关部分。
- 核对文档描述与当前实现是否一致，开始修改前简要说明本次目标、准备修改的模块或文件、修改边界以及可能需要同步的文档。说明本身不构成审批步骤；除非触发明确的确认条件，否则直接继续。
- 用户已明确要求或批准的行为、接口调整，视为已授权范围，不因其与旧实现或旧文档不同而重复确认。
- 只有发现超出已授权范围的变更，或无法可靠推断且会实质改变结果的关键选择时，才请求确认；只暂停依赖该决定的部分，继续完成其他已授权工作。
- 实现完成后再次对照相关文档检查改动。
- 代码导致架构、数据流、接口、运行方式或已记录行为发生变化时，同步更新已有文档；没有文档事实变化时不要为了形式修改或新增文档。

## 架构与函数

- 按算法职责和数据流划分模块，保持清晰的依赖方向。
- 顶层入口负责串联主要步骤，读起来应能直接看出完整算法流程。
- 一个函数只承担一个明确职责；函数名应直接表达它做什么或返回什么。
- 对作用不直观的函数，用简短注释说明用途、输入、输出和关键假设，不解释显而易见的实现细节。
- 函数的输入输出应尽量显式，避免隐藏的全局状态和难以追踪的副作用。
- 关键中间结果应容易查看，使问题能够沿“输入 → 中间步骤 → 输出”逐步排查。
- 算法核心与 ROS、底盘接口、文件读写和可视化等外部代码分开。
- 坐标系、时间戳、物理单位和关键参数应在使用处表达清楚。
- 内部实现以正确、直接、易读为准，不为潜在需求提前增加复杂抽象、通用框架或兼容层。
- 修改问题前先确定出错的模块或算法步骤，避免在无法定位原因时直接堆叠补丁。
- 只修改当前任务所需内容，不顺带重构无关代码；新增依赖前先征得用户同意。

## 测试

- 除非用户明确要求，否则不要编写、修改或运行任何测试，也不要搭建测试框架。
- 不主动运行已有测试、仿真、基准测试、覆盖率检查或手动测试。
- 读取代码和日志、检查差异，以及不执行项目业务代码的静态语法审阅，不属于本节所禁止的测试。
- 用户要求测试时，只执行其要求的范围；除非用户同时要求，不额外补充测试代码或测试体系。
- Skill 中的验证要求不得覆盖本节。没有测试授权时，完成其他已授权工作；不得擅自试跑脚本、启动仿真或执行硬件验证。
- 默认未测试，完成任务时明确说明这一点，不得暗示代码已经通过验证。

## 文档

文档分为面向人类和面向 Agent 两类，不要把两类内容混写在同一份文档中。

### 面向人类的文档

- 以 `README.md` 和常规项目文档为主，帮助读者理解项目目标、模块职责、算法流程以及如何运行和调试。
- 保持简要、连贯和易读，只解释当前有效的设计，不复制代码实现，不记录开发过程。
- 架构文档描述模块关系、主要数据流和关键接口；局部函数细节留在代码附近。
- 优先使用短段落和小型示意图。只有图表能明显降低理解成本时才使用。
- 更新已有文档而不是新建重复说明；除非用户明确要求，不创建进度报告、阶段总结或逐文件说明。

### 面向 Agent 的文档

- `AGENTS.md` 只保存工作规则和索引，具体排错资料按需放在 `docs/agent/` 中。
- 每次任务结束时，由主代理检查相关 Agent 文档是否需要同步。只有新增了符合记录条件的事实，或已有内容失效，且本次任务允许修改文件时才更新。纯问答、只读审阅，或没有符合上述条件的变化时不修改。
- Agent 文档服务于定位和修复问题，可记录代码路径与职责映射、输入输出、关键数据流、不变量、参数位置、运行命令和常见故障线索。
- 使用简短列表、表格、准确路径和命令，指向源代码而不是复制大段实现。
- 只记录经过确认、难以从代码直接推断并且会重复用于排错的信息；不要写推测、讨论过程或原始日志。
- 当代码路径、接口或命令变化时同步更新或删除过时内容，避免 Agent 被旧信息误导。
- 如果现有代码和 `AGENTS.md` 已足够排错，不要为了形式创建新的 Agent 文档。

## 文档索引

- `README.md`：项目入口、架构、阅读顺序和环境索引。
- `docs/algorithm.md`：当前算法状态机、Frontier、VLM 调用与恢复规则。
- `docs/chassis-interface.md`：Adapter 必须遵守的数据、坐标和执行契约。
- `docs/habitat.md`：Habitat 环境、GPU 启动和仿真运行。
- `docs/agent/habitat.md`：Habitat 启动顺序与 Rerun/GPU 快速隔离。
- `docs/hermes.md`：Hermes + D435i 安装、标定、运行、转发与排错。

`docs/slamtec-l515.md`、`docs/s100-l515.md`、`docs/orangepi.md` 已随范围收敛失效，
仅保留指向当前文档的说明，不再作为事实来源。
