# 本文档已失效

S100 底盘与 L515 相机的支持已从本项目移除：`adapters/s100_l515/`、
`s100-l515` 与 `calibrate-s100-l515` 入口、配套环境与启动脚本都不再是当前范围。
当前只保留 Habitat 仿真（[Habitat](habitat.md)）与 Hermes + D435i 真机
（[Hermes + D435i](hermes.md)）两条链路。

`adapters/realsense/` 中仍被 Hermes 标定复用的共享计算（RGB-D 采集基类、深度
地面拟合、视觉运动估计）保留；L515 专用的相机与 IMU 模块已移除。
相机硬件手册仍在 `wheeltech/` 下，作为硬件资料保留。
